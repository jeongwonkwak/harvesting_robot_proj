services:
  pi05-server:
    build:
      context: ../..
      dockerfile: serve/docker-19003/${DOCKERFILE:-Dockerfile.pi05}
    image: ${MODEL_NAME:-pi05-model}:latest
    container_name: vla-server-19003
    ports:
      - "${HOST_PORT:-19003}:8003"
    volumes:
      - ${MODEL_HOST_PATH:-../../models/public/pi05_base}:/models/vla-model:ro
      - ${STATS_HOST_PATH:-../../models/public/pi05_base}:/models/stats-model:ro
      - ./serve_pi05.py:/app/serve_pi05.py:ro
    ipc: host
    ulimits:
      memlock: -1
      stack: 67108864
    environment:
      MODEL_PATH: /models/vla-model
      MODEL_HOST_PATH: ${MODEL_HOST_PATH:-}
      STATS_PATH: /models/stats-model
      DEVICE: ${DEVICE:-cuda:0}
      CONVERT_ABS_TO_DELTA: ${CONVERT_ABS_TO_DELTA:-true}
      CUDA_VISIBLE_DEVICES: ${CUDA_VISIBLE_DEVICES:-0}
      PORT: "8003"
      HUGGING_FACE_HUB_TOKEN: ${HUGGING_FACE_HUB_TOKEN:-}
    runtime: nvidia
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8002/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 240s
